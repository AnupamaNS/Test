from django.conf.urls import url

from rating import views
#
urlpatterns = [
    # url(r'^view_rating/', views.view_rating),
    # url(r'^viert/', views.rateshop),
    # url(r'^rtpost/', views.postrate),

]